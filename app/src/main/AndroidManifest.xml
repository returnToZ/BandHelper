<?xml version="1.0" encoding="utf-8"?>
<manifest package="james.com.mag1c_band"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_app"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".UI.MainActivity"/>
        <activity android:name=".UI.SettingActivity"/>
        <activity android:name=".UI.PersonalInfoShowActivity"/>
        <activity android:name=".UI.MyDeviceActivity"/>
        <activity android:name=".UI.AboutActivity"/>
        <activity android:name=".UI.HelpActivity"/>
        <activity android:name=".UI.ServiceActivity"/>
        <activity android:name=".UI.RegisterActivity"/>
        <activity android:name=".UI.PersonalInfoEditActivity"/>
    </application>

</manifest>
